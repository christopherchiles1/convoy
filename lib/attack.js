(function(root) {
  'use strict';

  if (typeof root.CJS === "undefined") {
    root.CJS = {};
  }

  var Attack = CJS.Attack = function () {

  };

  Attack.prototype.move = function () {

  };

  Attack.prototype.draw = function () {

  };
}(this));
