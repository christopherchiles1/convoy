(function(root) {
  'use strict';

  if (typeof root.CJS === 'undefined') {
    root.CJS = {};
  }

  var View = CJS.View = function () {
    // create bg screen
    // create menu screen
  }

  View.prototype.start = function () {
    // set render tick
  };
}(this));
