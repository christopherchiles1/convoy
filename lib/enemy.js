(function(root) {
  'use strict';

  if (typeof root.CJS === "undefined") {
    root.CJS = {};
  }

  var Enemy = CJS.Enemy = function () {

  };

  Enemy.prototype.turn = function () {

  };
}(this));
