(function(root) {
  'use strict';

  if (typeof root.CJS === "undefined") {
    root.CJS = {};
  }

  var Level = CJS.Level = function () {

  };

  Level.prototype.spawn = function (first_argument) {
    // create enemies from level info and add them to game
  };

  Level.prototype.draw = function () {

  };
}(this));
