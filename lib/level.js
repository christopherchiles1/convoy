(function(root) {
  'use strict';

  if (typeof root.CJS === "undefined") {
    root.CJS = {};
  }

  var Level = CJS.Level = function () {

  };

  Level.prototype.createEnemies = function () {

  };

  Level.prototype.draw = function () {

  };
}(this));
