(function(root) {
  'use strict';

  if (typeof root.CJS === "undefined") {
    root.CJS = {};
  }

  var Item = CJS.Item = function () {

  };

  Item.prototype.draw = function () {
    
  };
}(this));
